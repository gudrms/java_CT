****# 🎯 NHN 면접 대비 필수 15문제 2주 완성 (Java)

> **목표**: 프로그래머스 핵심 유형 15문제 완벽 마스터
> **기간**: 2주 (하루 1-2시간)
> **타겟**: NHN 현장 라이브 코딩테스트 + 필기시험 + 면접
> **언어**: Java (구름IDE 환경)

## 🕐 NHN 면접 일정 (14:00~18:00)
```
14:00-16:00  라이브 코딩테스트 (2시간, 2-3문제)
16:00-16:30  필기시험 (30분, CS 지식)
16:30-18:00  면접 (코드리뷰 + 기술면접 + 인성)
```

---

## 📚 학습 방법

### 효율적인 뺑뺑이 전략 (NHN 라이브 코딩 대비)

**1회차**: 10분 고민 → 안 풀리면 답 보기 → 이해하기
**2회차**: 답 보면서 **Java로 타이핑** (손으로 익히기)
**3회차**: 답 안 보고 혼자 풀기 + **시간 재기** (목표: 문제당 30-40분)
**4회차**: 3일 후 다시 풀기 + **종이에 손코딩**
**5회차**: 1주일 후 **말하면서 코딩** (면접관에게 설명한다고 생각)

### 학습 팁 (NHN 특화)

✅ **"이해"보다 "암기"가 빠름**: 패턴 익히고 Java 구문 암기
✅ **답 보는 걸 부끄러워하지 마세요**: 10분 고민하고 안 풀리면 바로 답 보기
✅ **구름IDE에서 연습**: 자동완성 없는 환경 익숙해지기
✅ **말하면서 코딩 연습**: "지금 HashMap을 써서..." 중얼거리기
✅ **매일 기록하세요**: 진행상황 체크하면서 성취감 느끼기

### ⚠️ NHN 주의사항

- **Python 불가능 확률 높음** → Java만 연습
- **IDE 자동완성 없음** → 메서드명 정확히 암기 (`Arrays.sort()`, `Collections.sort()`)
- **손코딩 가능성** → 종이에 코드 써보기
- **말하면서 풀기** → 사고 과정 설명 필수

---

## 📝 필수 15문제 목록

### 1️⃣ 해시 (HashMap) - 3문제

#### ✅ 완주하지 못한 선수 🔥
- **난이도**: ⭐ (Lv.1.0) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42576
- **핵심 개념**: HashMap으로 카운팅, `getOrDefault()` 메서드
- **Java 키워드**: `HashMap<String, Integer>`, `getOrDefault()`, `keySet()`
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 전화번호 목록 🔥
- **난이도**: ⭐⭐ (Lv.2.0) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42577
- **핵심 개념**: HashSet으로 접두사 체크, `startsWith()` 활용
- **Java 키워드**: `HashSet<String>`, `startsWith()`, `substring()`
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 의상 🔥
- **난이도**: ⭐⭐ (Lv.2.0) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42578
- **핵심 개념**: HashMap 그룹핑, 조합 계산 (곱셈 공식)
- **Java 키워드**: `HashMap<String, Integer>`, `values()`, 수학 공식
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

---

### 2️⃣ 스택/큐 - 3문제

#### ✅ 기능개발 🔥
- **난이도**: ⭐⭐ (Lv.2.0) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42586
- **핵심 개념**: Queue로 순서 관리, 올림 계산 (`Math.ceil`)
- **Java 키워드**: `Queue<Integer>`, `LinkedList`, `Math.ceil()`, `poll()`
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 프린터 🔥
- **난이도**: ⭐⭐ (Lv.2.2) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42587
- **핵심 개념**: Queue + 우선순위, 인덱스 관리
- **Java 키워드**: `Queue<int[]>`, `Arrays.stream().max()`, `offer()`, `poll()`
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 다리를 지나는 트럭
- **난이도**: ⭐⭐ (Lv.2.5)
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42583
- **핵심 개념**: Queue로 시뮬레이션, 시간 흐름 구현
- **Java 키워드**: `Queue<Integer>`, `LinkedList`, 시간 관리
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

---

### 3️⃣ 완전탐색 (브루트포스) - 4문제

#### ✅ 모의고사 🔥
- **난이도**: ⭐ (Lv.1.0) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42840
- **핵심 개념**: 패턴 반복, 최댓값 찾기
- **Java 키워드**: `int[]` 배열, `%` 나머지 연산, `ArrayList<Integer>`
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 소수 찾기
- **난이도**: ⭐⭐ (Lv.2.5)
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42839
- **핵심 개념**: 순열 구현, 소수 판별
- **Java 키워드**: 재귀, `HashSet<Integer>`, `Math.sqrt()`, 에라토스테네스의 체
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 카펫
- **난이도**: ⭐⭐ (Lv.2.0)
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42842
- **핵심 개념**: 약수 찾기, 수학 공식 활용
- **Java 키워드**: `for` 반복문, 약수 계산, `int[]` 배열 반환
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 최소직사각형
- **난이도**: ⭐ (Lv.1.5)
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/86491
- **핵심 개념**: 정렬 후 최댓값 찾기
- **Java 키워드**: `Math.max()`, `Math.min()`, 2차원 배열
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

---

### 4️⃣ 정렬 - 3문제

#### ✅ K번째수 🔥
- **난이도**: ⭐ (Lv.1.0) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42748
- **핵심 개념**: 배열 복사, `Arrays.sort()` 함수
- **Java 키워드**: `Arrays.copyOfRange()`, `Arrays.sort()`, 인덱스 관리
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 가장 큰 수 🔥
- **난이도**: ⭐⭐ (Lv.2.5) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42746
- **핵심 개념**: 커스텀 정렬, Comparator 활용
- **Java 키워드**: `Arrays.sort()`, `Comparator`, `String.join()`, 문자열 비교
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ H-Index
- **난이도**: ⭐⭐ (Lv.2.2)
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42747
- **핵심 개념**: 정렬 후 조건 탐색
- **Java 키워드**: `Arrays.sort()`, 내림차순 정렬, 조건문
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

---

### 5️⃣ 그리디 - 2문제

#### ✅ 체육복 🔥
- **난이도**: ⭐ (Lv.1.5) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42862
- **핵심 개념**: 탐욕법, Set 연산
- **Java 키워드**: `HashSet<Integer>`, `contains()`, `remove()`, 그리디
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

#### ✅ 큰 수 만들기 🔥
- **난이도**: ⭐⭐ (Lv.2.4) **[중요]**
- **링크**: https://school.programmers.co.kr/learn/courses/30/lessons/42883
- **핵심 개념**: Stack + 그리디, 최적 선택
- **Java 키워드**: `Stack<Character>`, `push()`, `pop()`, `StringBuilder`
- **체크**: [ ] 1회차 [ ] 2회차 [ ] 3회차 [ ] 손코딩

---

## 💡 유형별 핵심 패턴 (Java)

### 해시 (HashMap)

**언제 사용?**
- 개수 세기
- 빠른 검색 필요 (O(1))
- 그룹핑

**핵심 메서드**
```java
// 1. HashMap 카운팅
HashMap<String, Integer> map = new HashMap<>();
map.getOrDefault(key, 0);  // 없으면 0 반환
map.put(key, map.getOrDefault(key, 0) + 1);

// 2. HashSet (중복 체크)
HashSet<String> set = new HashSet<>();
set.add(item);
set.contains(item);  // O(1) 검색
```

**자주 쓰는 패턴**
```java
// 패턴 1: 개수 세기
HashMap<String, Integer> map = new HashMap<>();
for (String item : array) {
    map.put(item, map.getOrDefault(item, 0) + 1);
}

// 패턴 2: 그룹핑
HashMap<String, ArrayList<String>> groups = new HashMap<>();
for (String[] pair : items) {
    String key = pair[0];
    String value = pair[1];
    if (!groups.containsKey(key)) {
        groups.put(key, new ArrayList<>());
    }
    groups.get(key).add(value);
}
```

---

### 스택/큐

**언제 사용?**
- 순서가 중요할 때
- LIFO(스택) 또는 FIFO(큐) 필요
- 괄호, 짝 맞추기

**핵심 구조**
```java
// 스택
Stack<Integer> stack = new Stack<>();
stack.push(item);  // push
stack.pop();       // pop
stack.peek();      // 최상단 확인

// 큐 (LinkedList 사용)
Queue<Integer> queue = new LinkedList<>();
queue.offer(item); // enqueue (add도 가능)
queue.poll();      // dequeue (remove도 가능)
queue.peek();      // 첫 원소 확인
```

**자주 쓰는 패턴**
```java
// 패턴 1: 조건 만족 시 제거
Stack<Integer> stack = new Stack<>();
for (int item : array) {
    if (!stack.isEmpty() && 조건) {
        stack.pop();
    } else {
        stack.push(item);
    }
}

// 패턴 2: 시뮬레이션
Queue<Integer> queue = new LinkedList<>();
int time = 0;
while (!queue.isEmpty()) {
    int current = queue.poll();
    time++;
    // 처리 로직
}
```

---

### 완전탐색 (브루트포스)

**언제 사용?**
- 데이터가 작을 때 (보통 10만 이하)
- 모든 경우를 다 봐야 할 때

**핵심 개념 (Java는 직접 구현)**
```java
// 순열 (재귀로 직접 구현)
void permutation(int[] arr, int depth, boolean[] visited, int[] output, int r) {
    if (depth == r) {
        // output 배열 처리
        return;
    }
    for (int i = 0; i < arr.length; i++) {
        if (!visited[i]) {
            visited[i] = true;
            output[depth] = arr[i];
            permutation(arr, depth + 1, visited, output, r);
            visited[i] = false;
        }
    }
}

// 조합 (재귀로 직접 구현)
void combination(int[] arr, int start, int depth, int r) {
    if (depth == r) {
        // 처리
        return;
    }
    for (int i = start; i < arr.length; i++) {
        combination(arr, i + 1, depth + 1, r);
    }
}
```

**자주 쓰는 패턴**
```java
// 패턴 1: 이중 반복문
for (int i = 0; i < n; i++) {
    for (int j = i + 1; j < n; j++) {  // i 다음부터
        // 검증 로직
    }
}

// 패턴 2: 재귀로 모든 경우 탐색
void dfs(int depth) {
    if (depth == target) {
        // 결과 처리
        return;
    }
    for (int i = 0; i < n; i++) {
        // 조건 체크
        dfs(depth + 1);
    }
}
```

---

### 정렬

**언제 사용?**
- 순서가 중요할 때
- 최소/최대 찾기
- 조건에 맞게 배열

**핵심 함수**
```java
// 1. Arrays.sort() - 배열 정렬
int[] arr = {3, 1, 2};
Arrays.sort(arr);  // 오름차순

// 2. Collections.sort() - 리스트 정렬
ArrayList<Integer> list = new ArrayList<>();
Collections.sort(list);  // 오름차순
Collections.sort(list, Collections.reverseOrder());  // 내림차순

// 3. Comparator로 커스텀 정렬
Arrays.sort(arr, (a, b) -> b - a);  // 내림차순
Arrays.sort(arr, (a, b) -> {
    // 복잡한 정렬 로직
    return 비교결과;
});
```

**자주 쓰는 패턴**
```java
// 패턴 1: 커스텀 정렬 (Comparator)
Arrays.sort(array, new Comparator<타입>() {
    @Override
    public int compare(타입 a, 타입 b) {
        // 오름차순: a - b
        // 내림차순: b - a
        return a - b;
    }
});

// 패턴 2: 문자열 비교 정렬
Arrays.sort(strings, (a, b) -> {
    String ab = a + b;
    String ba = b + a;
    return ba.compareTo(ab);  // 내림차순
});
```

---

### 그리디

**언제 사용?**
- 매 순간 최선의 선택
- 정렬 후 탐욕적 선택

**핵심 전략**
```java
// 1. 정렬 후 선택
Arrays.sort(array);
for (int item : array) {
    if (조건) {
        // 선택
    }
}

// 2. 큰 것부터 또는 작은 것부터
Arrays.sort(array, Collections.reverseOrder());  // 내림차순
```

**자주 쓰는 패턴**
```java
// 패턴 1: 최대한 많이 선택
int count = 0;
Arrays.sort(array);
for (int item : array) {
    if (조건) {
        count++;
    } else {
        break;
    }
}

// 패턴 2: 스택과 결합
Stack<Integer> stack = new Stack<>();
for (int item : array) {
    while (!stack.isEmpty() && stack.peek() < item) {
        stack.pop();
    }
    stack.push(item);
}
```

---

## 🔥 자주 쓰는 Java 코드 템플릿

### 입출력

```java
import java.util.*;

// Scanner 사용
Scanner sc = new Scanner(System.in);
int n = sc.nextInt();
String s = sc.next();
String line = sc.nextLine();

// BufferedReader (빠른 입력)
import java.io.*;
BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
int n = Integer.parseInt(br.readLine());
String[] input = br.readLine().split(" ");
```

### 배열/리스트 초기화

```java
// 1차원 배열
int[] arr = new int[n];
Arrays.fill(arr, 0);  // 모두 0으로 초기화

// 2차원 배열
int[][] arr = new int[n][m];

// ArrayList
ArrayList<Integer> list = new ArrayList<>();
list.add(1);
list.get(0);
list.size();

// 배열 → 리스트
Integer[] arr = {1, 2, 3};
ArrayList<Integer> list = new ArrayList<>(Arrays.asList(arr));
```

### 자주 쓰는 함수

```java
// 최댓값/최솟값
int max = Arrays.stream(arr).max().getAsInt();
int min = Arrays.stream(arr).min().getAsInt();
int max = Collections.max(list);
int min = Collections.min(list);

// 올림/내림/반올림
Math.ceil(3.2);   // 4.0 (올림)
Math.floor(3.8);  // 3.0 (내림)
Math.round(3.5);  // 4 (반올림)

// 절댓값, 제곱, 제곱근
Math.abs(-5);     // 5
Math.pow(2, 3);   // 8.0
Math.sqrt(16);    // 4.0
```

### 문자열

```java
// 분리/결합
String[] parts = str.split(" ");  // 공백 기준
String[] parts = str.split(",");  // 쉼표 기준
String joined = String.join("", parts);  // 배열 → 문자열

// 변환
str.toUpperCase();  // 대문자
str.toLowerCase();  // 소문자
str.trim();         // 양쪽 공백 제거

// 검사
str.startsWith("A");  // A로 시작
str.endsWith("Z");    // Z로 끝남
str.contains("B");    // B 포함
Character.isDigit(c); // 숫자인지
Character.isLetter(c);// 문자인지

// 문자 <-> 숫자
int num = '5' - '0';  // 5
char ch = (char)(3 + '0');  // '3'
```

### Set 연산

```java
HashSet<Integer> set1 = new HashSet<>(Arrays.asList(1, 2, 3));
HashSet<Integer> set2 = new HashSet<>(Arrays.asList(2, 3, 4));

// 합집합
HashSet<Integer> union = new HashSet<>(set1);
union.addAll(set2);  // {1, 2, 3, 4}

// 교집합
HashSet<Integer> intersection = new HashSet<>(set1);
intersection.retainAll(set2);  // {2, 3}

// 차집합
HashSet<Integer> difference = new HashSet<>(set1);
difference.removeAll(set2);  // {1}
```

---

## ✅ NHN 면접 대비 학습 체크리스트

### Week 1: 알고리즘 패턴 익히기 (1-3회차)

- [ ] **Day 1-2**: 해시 3문제 (Java로 구현, getOrDefault 암기)
- [ ] **Day 3-4**: 스택/큐 3문제 (Stack, Queue 구조 익히기)
- [ ] **Day 5-6**: 완전탐색 4문제 (순열 재귀 구현 연습)
- [ ] **Day 7**: 정렬 3문제 + 그리디 2문제 (Comparator 활용)

### Week 2: 실전 대비 + 속도 올리기 + 필기 준비

- [ ] **Day 8-10**: 전체 15문제 **시간 재고 다시 풀기** (문제당 30-40분)
- [ ] **Day 11**: 막혔던 문제 집중 공략 + **종이에 손코딩**
- [ ] **Day 12**: **2시간 모의고사** (프로그래머스 Lv.2 랜덤 2-3문제)
- [ ] **Day 13**: **필기시험 대비** (아래 CS 지식 정리)
- [ ] **Day 14**: 전체 복습 + **말하면서 코딩 연습**

### NHN 필기시험 대비 (30분, Day 13)

- [ ] **자료구조**: HashMap/ArrayList 시간복잡도, 스택/큐 차이
- [ ] **알고리즘**: 빅오 표기법, 정렬 알고리즘 종류 (O(nlogn) vs O(n²))
- [ ] **운영체제**: 프로세스 vs 스레드, 교착상태 (Deadlock)
- [ ] **네트워크**: TCP vs UDP, HTTP 메서드 (GET/POST)
- [ ] **데이터베이스**: ACID, 정규화, 인덱스 개념

### 최종 목표 (면접 당일 체크)

- [ ] 15문제 모두 **Java로** 답 안 보고 풀 수 있음
- [ ] 구름IDE 환경에서 **자동완성 없이** 코딩 가능
- [ ] **말하면서 코딩** 가능 ("지금 HashMap을 사용해서...")
- [ ] **손코딩** 가능 (종이에 코드 작성 가능)
- [ ] 2시간 안에 2-3문제 풀 수 있는 속도
- [ ] CS 기본 개념 설명 가능

---

## 📊 진행상황 트래킹

### 완료한 문제 수

- 해시: _____ / 3
- 스택/큐: _____ / 3
- 완전탐색: _____ / 4
- 정렬: _____ / 3
- 그리디: _____ / 2
- **합계: _____ / 15**

### 반복 횟수별 완료

- 1회차 (답 보고 이해): _____ / 15
- 2회차 (따라 치기): _____ / 15
- 3회차 (혼자 풀기): _____ / 15
- 4회차 (3일 후): _____ / 15
- 5회차 (1주 후): _____ / 15

---

## 💪 NHN 면접 합격 응원 메시지

**2주 투자 = NHN 합격**

- 하루 1-2시간 × 14일 = 총 14-28시간
- 이 시간이면 충분합니다!
- Java 개발자가 Java로 푸니까 유리합니다

**작은 성공을 축하하세요!**

- "오늘 Java로 3문제 풀었다" ✅
- "어제 못 풀던 거 오늘 손코딩으로 풀었다" 🎉
- "30분 만에 풀었다" 🚀
- "말하면서 코딩했다" 💬

**면접 당일 당신은:**

- 15문제 패턴을 완벽히 암기한 상태
- 2시간 라이브 코딩 자신있게 대응
- 필기시험 CS 지식 설명 가능
- 면접관과 소통하며 코딩 가능
- **NHN 합격!** 🎊

**D-14일! 지금 바로 시작하세요!**

1. 구름IDE 회원가입
2. "완주하지 못한 선수" 문제 Java로 풀기
3. 매일 체크리스트 표시하며 성취감 느끼기

---

**화이팅! 💪 NHN 면접 성공을 응원합니다!**
소수 찾기 (순열 구현 어려움)
카펫 (수학 문제)
최소직사각형 (너무 쉬움)
H-Index (복잡함)
다리를 지나는 트럭 (시뮬레이션 복잡)